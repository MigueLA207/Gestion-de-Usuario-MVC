@model TestMVC.ViewModel.OrderC

<h1 class="text-center">Gestion de Ordenes</h1>
<a  asp-controller="Users" asp-action="Create" class="btn btn-info mb-3">Crear Nueva Orden</a>
<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle">
                <thead>
                <th>Id</th>
                <th>Order Number</th>
                <th>Date</th>
                <th>Client</th>
                </thead>
                <tbody>
                @foreach (var Order in Model.Orders)
                {
                    <tr>
                        <td>@Order.Id</td>
                        <td>@Order.OrderNumber</td>
                        <td>@Order.Date</td>
                        <td>@Order.Client.Name @Order.Client.LastName</td>
                        <td>
                            <div class="d-flex flex-column flex-sm-row gap-2">
                                <a asp-controller="Order" asp-action="Details" asp-route-id="@Order.Id" class="btn btn-info btn-sm">Detalles</a>
                                @* <a asp-controller="Users" asp-action="Edit" asp-route-id="@Order.Id" class="btn btn-warning btn-sm">Editar</a> *@
                                @* <a asp-controller="Users" asp-action="Delete" asp-route-id="@Order.Id" class="btn btn-danger btn-sm">Eliminar</a> *@
                            </div>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
</div>